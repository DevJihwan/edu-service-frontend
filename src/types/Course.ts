// types 디렉토리에 Course.ts 파일 생성

export interface Course {
    _id: string;
    title: string;
    info: string;
    location: string;
    date: string;
    time: string;
    price: string;
    sessionType: string;
    // 필요한 다른 필드들도 여기에 추가하세요
  }
  